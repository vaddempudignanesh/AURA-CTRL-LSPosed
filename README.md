# AURA-CTRL-LSPosed
AURA CTRL — LSPosed module for Android 13+ providing system-wide activity, window &amp; list animations, status bar tweaks, Always-On Display (AOD), gesture triggers, and boot animations. Works across system and third-party apps without modifying app code.
